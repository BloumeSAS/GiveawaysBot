services:
  giveaways-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bloumechat-giveaways-bot
    restart: unless-stopped
    volumes:
      - giveaways-data:/app/data
    env_file:
      - .env

volumes:
  giveaways-data:
